import 'package:flutter/material.dart';
class Palette{
  Palette._();
  static const Color primaryBlue = Color(0xFF0D47A1);   // A strong, dark blue
  static const Color accentOrange = Color(0xFFFF9800);  // An orange for highlights
  static const Color surfaceGray = Color(0xFFF5F5F5);   // A light gray for text field backgrounds
  static const Color textBlack = Color(0xFF212121);    // For primary text
  static const Color textGray = Color(0xFF757575);     // For secondary text or hints
  static const Color white = Colors.white;             // Using a standard color
}